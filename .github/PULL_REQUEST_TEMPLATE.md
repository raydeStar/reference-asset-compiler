## What changed

<!-- One or two sentences. Link the AGENT_TASKS item, issue or HANDOFF entry it serves. -->

## Checklist

- [ ] Ran `.\scripts\verify.ps1` and it ended with `RAC_VERIFY_OK`. Paste that line:

  ```text

  ```

- [ ] No generated production assets, model weights, licensed add-ons, Unreal content or machine-local paths are committed. Small test/example fixtures carry an explicit license.
- [ ] Documentation updated where behaviour, flags or paths changed (`docs/`, `README.md`, `CLAUDE.md`).
- [ ] If behaviour changed: a dated entry appended to `docs/HANDOFF.md`, and a `docs/DECISIONS.md` lesson if something failed or surprised you.
- [ ] No asset approval status, waiver, or evidence JSON under `docs/evidence/` changed unless this PR is the review that changes it, and the reviewer is named.
